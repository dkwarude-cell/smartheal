/**
 * Firebase Configuration for React Native
 * SmartHeal (Runverve) - Firebase Integration
 */

// Firebase removed. Stub exports to satisfy imports without bundling Firebase.
export const app = null as any;
export const auth = null as any;
export const firestore = null as any;
export default app;
